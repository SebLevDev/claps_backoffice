<form action="{{ path('app_video_search') }}" class="search-bar">
    <input type="search" name="search" placeholder="Rechercher une vidéo…" value="{{ app.request.query.get('search') }}">
    <button type="submit" class="btn btn-primary btn-sm">Rechercher</button>
</form>
