{# HERO — vidéo plein écran en boucle + baseline et 1 CTA en superposition.
   Fichiers dans public/videos/ (dérivés de ClapSabots-Presentation.mp4, extrait ~1:12, filigrane tvcom flouté) :
     - hero.mp4         (1600×900, ~14 s, sans son)
     - hero-poster.jpg  (image d'attente + fallback mobile / prefers-reduced-motion) #}
<section id="hero" style="background-image:url('{{ asset('videos/hero-poster.jpg') }}')">
	<video class="hero-video" autoplay muted loop playsinline preload="auto"
	       poster="{{ asset('videos/hero-poster.jpg') }}">
		<source src="{{ asset('videos/hero.mp4') }}" type="video/mp4">
	</video>
	<div class="hero-overlay"></div>
	<div class="hero-content">
		<p class="hero-eyebrow">Braine-l'Alleud · Danses traditionnelles depuis 1977</p>
		<h1 class="display">La danse du monde,<br><em>ici, ensemble.</em></h1>
		<a href="{{ path('app_sections_list') }}" class="btn btn-primary">Nos sections →</a>
	</div>
</section>
